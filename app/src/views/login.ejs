<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Login</title>
    <link href="/static/css/login.css" rel="stylesheet" type="text/css" />
    <link
      rel="shortcut icon"
      type="image/x-icon"
      href="/static/image/logo-ACD.png"
    />
    <link
      href="https://fonts.googleapis.com/css?family=Major Mono Display"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="title">
      <form action="/login" method="POST">
        <h1>Connexion</h1>
        <% if (error_msg != "") { %>
        <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>
        <div class="info">
          <div class="Username">
            <input
              type="text"
              class="login-field"
              placeholder="Nom d'utilisateur"
              id="Username"
              name="username"
            />
            <label class="login-field-icon fui-user" for="Username"></label>
          </div>
          <div class="Password">
            <input
              type="password"
              class="login-field"
              placeholder="Mot de passe"
              name="password"
              id="password"
            />
            <label class="login-field-icon fui-lock" for="password"></label>
          </div>
          <div class="container">
            <a class="forgotPassword" href="#">Mot de passe oublier ?</a>
            <button type="submit" class="login" value="submit">Log in</button>
            <a class="createCompte" href="/register">Créer un compte</a>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
