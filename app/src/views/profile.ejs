<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <script src="https://unpkg.com/maplibre-gl@^5.1.0/dist/maplibre-gl.js"></script>
    <link
      href="https://unpkg.com/maplibre-gl@^5.1.0/dist/maplibre-gl.css"
      rel="stylesheet"
    />
    <link
      rel="shortcut icon"
      type="image/x-icon"
      href="/static/image/logo-ACD.png"
    />
    <link
      href="https://fonts.googleapis.com/css?family=Major Mono Display"
      rel="stylesheet"
    />
    <style>
      .user-profile {
        display: flex;
        background-color: #000000;
        color: white;
        padding: 20px;
      }
      .img-prenom {
        width: 50%;
        margin: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      .img-prenom img {
        max-width: 200px;
        height: auto;
      }

      .navigation-links {
        position: absolute;
        display: flex;
        flex-direction: column;
        height: 300px;
        justify-content: space-between;
        left: 0;
      }
      .info {
        color: white; /* Add text color for links */
        text-decoration: none; /* Remove underline */
        text-align: left;
        padding: 15px;
        text-align: center; /* Corrected property name from align-text to text-align */
        background-color: #13255b;
        padding-right: 65px;
        border-radius: 0px 70px 70px 0px;
        transition: transform 0.3s ease-in-out;
      }
      .infperso {
        color: white; /* Add text color for links */
        text-decoration: none; /* Remove underline */
        text-align: center; /* Corrected property name from align-text to text-align */
        padding: 5px 60px 5px 20px;
        text-align: center; /* Corrected property name from align-text to text-align */
        background-color: #13255b;
        border-radius: 0px 70px 70px 0px;
        transition: transform 0.3s ease-in-out;
      }
      .profile {
        background-color: #2c54ce;
      }
      .navigation-links a {
        transition: 0.4s;
      }
      .navigation-links a:hover {
        background-color: #2c54ce;
        transform: scale(1.1) translateX(8px);
      }
      .titre-carte-visité,
      .section-progression h1 {
        margin-top: 40px;
        text-align: center;
      }
      /* Section carte */
.map-section {
  text-align: center;
  margin-top: 32px;
  margin-bottom: 24px;
}

#map-area {
  display: flex;
  flex-direction: column;
  align-items: center;
  background: #f8f9fa;
  border-radius: 18px;
  box-shadow: 0 4px 24px rgba(44, 84, 206, 0.08);
  padding: 24px 0 32px 0;
  margin-bottom: 32px;
}

#map {
  width: 1036px;
  height: 621px;
  border-radius: 14px;
  overflow: hidden;
  box-shadow: 0 2px 12px rgba(44, 84, 206, 0.10);
  margin-bottom: 16px;
  background: #e6eaf6;
  position: relative;
}

.map-image {
  max-width: 90%;
  height: auto;
  border: 2px solid #ccc;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  border-radius: 10px;
}

#switch-map-button {
  position: absolute;
  top: 18px;
  left: 18px;
  z-index: 2;
  padding: 7px 18px;
  background-color: #13255b;
  color: #fff;
  border: none;
  border-radius: 7px;
  font-weight: 600;
  font-size: 1rem;
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(44, 84, 206, 0.08);
  transition: background 0.2s;
}

#switch-map-button:hover {
  background-color: #2e4999;
}

.maplibregl-popup {
  max-width: 400px;
  font: 13px/20px "Helvetica Neue", Arial, Helvetica, sans-serif;
  max-height: 60px;
}

.maplibregl-popup-content {
  max-width: 400px;
  font: 13px/20px "Helvetica Neue", Arial, Helvetica, sans-serif;
  border-radius: 8px;
  background: #fff;
  color: #222;
  padding: 14px 18px;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  gap: 10px;
  box-shadow: 0 2px 12px rgba(44, 84, 206, 0.10);
}

.maplibregl-popup-content button {
  border-radius: 6px;
  background-color: #2c54ce;
  color: #fff;
  border: none;
  padding: 6px 14px;
  font-size: 0.95rem;
  cursor: pointer;
  transition: background 0.2s;
}

.maplibregl-popup-content button:hover {
  background-color: #13255b;
}
      .carte-favoris, .carte-historique {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 1036px;
        padding-top: 20px;
        padding-bottom: 20px;
        background-color: #13255b;
        color: white;
        text-align: left;
        border-radius: 20px;
        margin-top: 20px;
        border: none;
        cursor: pointer;
      }
      .hisfav h2 {
        padding-left: 20px;
      }
      .hisfav img {
        margin: auto;
        height: 25px;
        width: 25px;
        padding-right: 20px;
        transition: transform 0.3s ease, opacity 0.3s ease; 
        transform-origin: center; 
      }
      .hisfav {
        display: flex;
        justify-content: center;
        flex-direction: column;
        align-items: center;
      }
      .rotated {
        margin-top: 2%;
        transform: scaleY(-1); 
      }
      .showFav, .showHist {
        height: 200px;
        width: 1036px;
        align-items: normal;
      }
      .favoImage, .favoHist{
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 1036px;
        height: 25px;
      }

      /* Chart.js styles */
      .chart-container {
        position: relative;
        width: 100%;
        max-width: 600px;
        margin: 0 auto;
      }

      /* Responsive styles */
      @media (max-width: 768px) {
        .user-profile {
          flex-direction: column;
          align-items: center;
        }
        .img-prenom {
          width: 100%;
        }
        .navigation-links {
          position: static;
          flex-direction: row;
          flex-wrap: wrap;
          justify-content: center;
          margin-top: 20px;
        }
        .info, .infperso {
          padding: 10px;
          width: auto;
          margin: 5px;
        }
        #map {
          width: 100%;
          height: 400px;
        }
        .carte-favoris, .carte-historique {
          flex-direction: column;
          align-items: flex-start;
          width: 100%;
          padding: 15px;
        }
        .hisfav h2 {
          font-size: 18px;
        }
        .hisfav img {
          height: 20px;
          width: 20px;
        }
      }
    </style>
  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>

</head>
  <body >
    <div id="app">
    <%- include('partials/Header', { user: user }) %>

    <section class="user-profile">
      <div class="navigation-links">
        <a href="/profile" class="info profile">Mon profil</a>
        <a href="/infouser" class="infperso"
          >Informations <br />
          personnelles</a
        >
        <a href="/security" class="info">Sécurité</a>
        <a href="/forum" class="info">Forum</a>
      </div>
      <div class="img-prenom">
        <img  :src="imageProfile" alt="User Profile Picture" />
        <h1>Bienvenue <%= user.username %> !</h1>
        <p>Consultez votre historique de visites et gérez vos favoris !</p>
      </div>
    </section>


    <section class="section-lieux-visités">
      <h1 class="titre-carte-visité">Votre carte des vos sites visités</h1>
      <div id="map-area">
        <div id="map" style="width: 1036px; height: 621px; z-index: 0"><button id="switch-map-button" onclick="switchMaps()">3d</button></div>
        <div id="details-area">
          <div id="site_title"></div>
          <div id="site_description"></div>
          <div style="display: none;" id="favoriteId"></div>
          <button style="display: none" id="favoriteButton" onclick="addToFavorites()">Ajouter à la wishlist</button>
        </div>
      </div>
    </section>
    <div class="hisfav">
      <button class="carte-favoris" @click="changeImagefav" onclick="showFavorites()">
        <div class="favoImage">
          <h2>Wishlist</h2>
          <div><img class="fav-image" src="/static/image/arrow-bottom.png"alt="next" /></div>
        </div>
        <div class="favoris" v-if="showFav">
          <p>ASDSA</p>
        </div>
      </button>
      <button class="carte-historique" @click="changeImageHist" onclick="showHistorique()">
        <div class="favoHist">
        <h2>Historique</h2>
        <div><img class="hist-image" src="/static/image/arrow-bottom.png" alt="next" /></div>
      </div>
      <div class="favoris" v-if="showFav">
        <p>ASDSA</p>
      </div>
    </button>

    <section class="section-progression">
      <h1>Votre progression de visites dans le monde</h1>
      <button id="camembert-back-button" onclick="camembertbackbutton()">←</button>
      <canvas id="pieCanvas" aria-label="chart" role="img"></canvas>
    </section>

    <script type="module" src="/static/map-key.js"></script>
    <script type="module" src="/static/mapv2.js"></script>
    <%- include('partials/Footer')%>
  </div>
  
  <!-- Import App -->
  <script src="/static/js/main.js"></script>
  <!-- Mount App -->
  <script>
    const mountedApp = app.mount('#app');
  </script>
  <script>
    window.chartDataFromServer = <%- JSON.stringify(chartData) %>;
    window.userFromServer = <%- JSON.stringify(user) %>;
  </script>
  <!-- Chart.js depuis CDN -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <!-- Ton script -->
  <script src="/static/js/chart.js"></script>
  </body>    
</html>
